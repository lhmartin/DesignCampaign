name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-windows:
    runs-on: windows-2022
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22.x'
          cache: npm
          cache-dependency-path: designcampaign-web/package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: designcampaign-web

      - name: Build Windows
        run: npm run typecheck && npm run prepare:uv && npm run build:vite && npx electron-builder --win --publish never
        working-directory: designcampaign-web

  build-mac:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22.x'
          cache: npm
          cache-dependency-path: designcampaign-web/package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: designcampaign-web

      - name: Build macOS
        run: npm run typecheck && npm run prepare:uv && npm run build:vite && npx electron-builder --mac --publish never
        working-directory: designcampaign-web

  build-linux:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22.x'
          cache: npm
          cache-dependency-path: designcampaign-web/package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: designcampaign-web

      - name: Build Linux
        run: npm run typecheck && npm run prepare:uv && npm run build:vite && npx electron-builder --linux --publish never
        working-directory: designcampaign-web
