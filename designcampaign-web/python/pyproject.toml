[project]
name = "designcampaign-sidecar"
version = "0.1.0"
description = "Python sidecar environment for DesignCampaign"
requires-python = ">=3.11"
dependencies = [
    "antpack>=0.3",
]

[tool.uv]
python-preference = "only-managed"
python-downloads  = "automatic"
